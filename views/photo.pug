extends layout

block content
    p Welcome to Photos
    .photo-upload
        form(action="/photo/upload", method="post", enctype="multipart/form-data")
            input(name="avatar", type ="file")
            input(type="submit")