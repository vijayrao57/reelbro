function checkFileAuthenticty(file){
    
}

function checkMimeType(file){
    if(file.avatar.mimetype == 'image/jpeg' || 'image/png'){
        return true;
    }else{
        return false;
    }
}

function CheckSize(){
    
}